# Registro de avance · tanda del 2026-09-24

Para retomar el trabajo si se interrumpe. Cada línea dice qué quedó hecho, en qué
commit y qué falta. Rama: `clinical-encounter-v0.13`. Nada se fusionó a `main` y nada
se publicó en la app pública.

## Entorno de esta sesión (nube)

- Sin `OPENAI_API_KEY`, sin `MRS_DATABASE_URL`, sin `.streamlit/secrets.toml`.
- La política de red rechaza `api.openai.com` y `clinical-management-reasoning-dev.streamlit.app`
  (el proxy responde 403). Neon resuelve por DNS, pero no hay credenciales.
- Consecuencia: aquí no se puede hacer ninguna corrida pagada, ni entrar con
  `residente_prueba_r3`, ni ver la base persistente, ni regenerar los 45 PDF de la tanda
  anterior (sus registros viven en la base o en `local-data/`, fuera del repositorio).
- La suite corre completa sin red: `python -m pytest -q` y `python run_regressions.py`.

## Sesión del 2026-09-25 (tarde): intento de ejecutar la tanda, detenido en la verificación previa

- Accesos presentes: las tres variables `MRS_BATCH_*` están en el entorno; el dominio
  `clinical-management-reasoning-dev.streamlit.app` responde a través del proxy
  (`/~/+/_stcore/health` → 200).
- Chromium no confiaba en la CA del proxy (`ERR_CERT_AUTHORITY_INVALID`): el almacén NSS
  `/root/.pki/nssdb` estaba vacío. Se resolvió agregando `/root/.ccr/agent-proxy-ca.crt`
  con `certutil` (paquete `libnss3-tools`), sin desactivar la verificación TLS. Es
  configuración del contenedor: una sesión nueva debe repetirlo.
- **Bloqueo**: la raíz de la app responde `303` hacia
  `https://share.streamlit.io/-/auth/app?...` (autenticación de Streamlit Community Cloud
  antes de servir la app) y la política de red del entorno rechaza `share.streamlit.io`
  (el proxy responde 403 al CONNECT). Sin esa cookie, `/_stcore/stream` responde 401.
  Chromium termina en `ERR_TUNNEL_CONNECTION_FAILED`; la verificación previa no llegó a
  iniciar sesión con ninguna cuenta.
- Riesgo siguiente, no verificable aún: la app usa WebSocket (`/_stcore/stream`) y la
  documentación del proxy del entorno dice que no admite WebSocket. Si al permitir
  `share.streamlit.io` la página carga pero no se conecta, ese es el motivo.
- Consumo: **0 de 40** encuentros pagados; no se creó `local-data/tanda20/batch/ledger.jsonl`.
  No se cambió código.

## Hecho

Cada commit se empuja sólo después de pasar la suite completa y las regresiones en una
copia limpia de la rama (por ejemplo, 3814 pruebas y 56/56 regresiones sobre `7cb7536`).

| # | Qué | Commit |
|---|---|---|
| 1 | §3a/§3b/§3c: veredicto obligatorio por cada evento definido (prompt de rúbrica 1.1), verificación determinista del registro (`rubric_screening.py`), marcas de desacuerdo propuesta/registro, «no evaluable» ofrecido cuando la ventana del dominio no se abrió, confirmar un evento contradicho exige razón escrita | `27490f7` |
| 2 | Identificadores internos en la prosa: conversión centralizada (`report_presentation.humanize`), numeración real de decisiones en tres lugares que sumaban uno al índice, cita textual del residente sin tocar | `b728a46` |
| 3 | Estudios no modelados (grupo y pruebas cruzadas, TC de cerebro): se registra la solicitud, nunca un resultado inventado ni «no disponible»; el resto de la entrega se ejecuta (`docs/CAPACIDADES_PENDIENTES.md`) | `b06f506`, `bd7a89f` |
| 4 | Contexto de asistencia y autonomía (§1-10): declaración separada de la autonomía, «no informado» válido, ejecución sintética registrada como tal, brief 1.4 con `assistance_snapshot` y `autonomy_withheld`, frase de asistencia en el idioma del lector | `af3de97`, `ac5cfc7` |
| 5 | `MRS_AI_CUES=held`: semántica, transiciones, persistencia al reanudar (también del presupuesto), regla de observación, sin efecto en brief ni rúbrica | `379e551` |
| 6 | Reproducibilidad del puntaje: encuentro determinista (12/12), arnés de traza congelada sin caché, anclas y marcas de cita (`docs/REPRODUCIBILIDAD_PUNTAJE.md`) | `eda06e7` |
| 7 | Lo que el ensayo de la tanda encontró en la escritura diaria de un residente («nbz», «vvp», «SF 1000 ml ev», no recirculación, resultados leídos, medicamentos del paciente, hemodinamia, observación, fármacos no modelados, tratamientos sin las cuatro preguntas) | `1b48b09` |
| 8 | Tanda de 20: selección, guiones y ensayo offline por la página real (20/20) | `0631ca4` |
| 9 | Perfil acumulado: una sola versión de rúbrica, resultados individuales con fecha, lo que espera confirmación, alertas; columna «Awaiting your review» | `3ee0093` |
| 10 | Documento de rúbrica impreso desde la propuesta, antes de la decisión (sólo docentes) | `2561aa7` |
| 11 | Un docente dirige el próximo caso de un residente (una vez, con motivo, trazable) | `bf08933` |
| 12 | Cada encuentro registra la versión del código que lo lanzó | `7cb7536` |
| 13 | Ejecutor de la tanda por navegador (Playwright), verificado contra una copia local; dependencia de desarrollo | `e0caf50`, `7f0d869` |
| 14 | Cierre: un alta retenida se nombra como alta; la regla de generación pagada recibe el rol de quien lanza; `docs/TANDA_20_ESCENARIOS.md`; `docs/DECISIONES_CLINICAS_PENDIENTES.md`; este registro | `65e9f04`, `7e0f908` |
| 15 | Un encuentro cerrado ya no se examina ni adquiere ECG: examinar después del cierre agregaba una decisión al registro guardado y corría el reloj clínico (y con él el minuto de cierre que lee el cribado de la rúbrica); el brief docente, los objetivos y la rúbrica leen ese registro | `2af52b0` y anteriores |
| 16 | Sus 15 decisiones del 2026-09-25, una por commit, con pruebas (detalle y lo que queda a su criterio: `docs/DECISIONES_CLINICAS_PENDIENTES.md`, al inicio) | `e97a4b9` … `fe6781b` |
| 17 | Clasificación de encuentros: documentos, nuevo análisis o nueva ejecución, decidida desde los registros (`tools_reclassify.py`) | `db67698` |
| 18 | Los 20 guiones en inglés (`tanda20_en.py`), el lector inglés corregido donde no leía lo mismo que el español, semilla fijable en el ensayo, guion 7 sin depender del vómito aleatorio | `ed709e6` |

## Pendiente real

- **La tanda de 20 no se ejecutó**: 0 de 40 encuentros pagados usados (intento del
  2026-09-25 detenido en la verificación previa: `share.streamlit.io` bloqueado, ver arriba). Bloqueos y pasos
  exactos en `docs/TANDA_20_ESCENARIOS.md` §4 (red hacia la app de desarrollo, contraseña
  de `residente_prueba_r3` y una cuenta docente de prueba como variables de entorno, la
  app de desarrollo en esta rama, `MRS_SYNTHETIC_ACCOUNTS`).
- **Reprocesar los nueve encuentros de la tanda de diez y regenerar sus 45 PDF**, y el
  antes/después de los diez: sus registros viven en la base o en `local-data/`, fuera de
  este contenedor.
- Al reprocesar esos nueve encuentros, comprobar si alguno tiene entradas después del
  cierre (traza guardada más larga que la congelada al cerrar): hasta el fix 15 un examen
  posterior al cierre entraba en lo que leen el brief, los objetivos y la rúbrica.
- **Medir la variabilidad del evaluador** (5-10 solicitudes) y comparar las corridas
  13/15 y 9/15 con `tools_rubric_reproducibility.py --compare` (identidad primero; gasto
  en un registro aparte). Bloqueado: sin clave y sin esas dos corridas aquí.
- **Clasificar los nueve encuentros de la tanda de diez** con
  `tools_reclassify.py --database "$MRS_DATABASE_URL" --account <cuenta> --since <revisión>`
  donde esté la base (lectura, sin escribir nada).
- **Lo que queda a su criterio tras las decisiones** (tabla al inicio de
  `docs/DECISIONES_CLINICAS_PENDIENTES.md`): pesos verificados por caso, revisión de las
  ventanas, lista de intervenciones urgentes, objetivos con autonomía exigida, estudios
  a incorporar, autorizar la cuenta docente de prueba.

## Para retomar

1. `git checkout clinical-encounter-v0.13 && pip install -r requirements-dev.txt`.
2. `python -m pytest -q` y `python run_regressions.py` (sin red).
3. `python tools_tanda20.py --plan`; `--rehearse all` para repetir el ensayo gratis
   (`--seed N` para fijar la semilla del encuentro; `--language en` con las órdenes en
   inglés).
4. Con los accesos de `docs/TANDA_20_ESCENARIOS.md` §4 —el dominio de la app de
   desarrollo permitido en la red del entorno y `MRS_BATCH_RESIDENT_PASSWORD`,
   `MRS_BATCH_STAFF_USER`, `MRS_BATCH_STAFF_PASSWORD` como variables del entorno (las lee
   una sesión nueva)—: `python tools_tanda20.py --local-check 1` (gratis, copia local),
   `python tools_tanda20.py --preflight --base-url <app de desarrollo>` (gratis, app real,
   sólo lee) y, cuando todo esté «ok», `--run 1 --base-url <app de desarrollo>`; seguir
   con el registro `local-data/tanda20/batch/ledger.jsonl`.
5. Estado de las cuentas (2026-09-25, captura del administrador): `residente_prueba_r3`
   está en la misma base que la cuenta de administrador; falta crear la cuenta docente
   de prueba y autorizarla para `residente_prueba_r3`.
